{% extends 'index.html.twig' %}


{% block body %}
	<ul>

		<img class="profilePitcure" src="{{ ( asset(profilPicture)) }}">

		<li>Nom :
			{{ character.name }}</li>
		<li>Niveau :
			{{ character.level }}</li>
		<li>Date de naissance :
			{{ character.birthdate | date('d-m-y') }}</li>
		<li>Description :
			{{ character.description }}</li>
		<li>Classe :
			{{ character.type }}</li>
		<li>Compétences :
			<ul>
				{% for skill in character.skill %}
					<li>
						{{ skill }}
					</li>
				{% endfor %}
			</ul>
		</li>
		<li>experience :
			{{ character.experience }}
			xp</li>
		<li>Vie :
			{{ character.health }}
			pv</li>
	</ul>

	<a class="margin1" href="/character/delete/{{ character.id }}">supprimer</a>
	<a class="margin1" href="/character/edit/{{ character.id }}">modifier</a>
	<p></p>
	<a class="margin1" href="/">Home Page</a>
{% endblock %}
